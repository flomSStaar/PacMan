package model.entity.ghost;

import model.entity.BaseEntity;

public class Ghost extends BaseEntity {
    public Ghost(float x, float y, float width, float height)
    {
        super(x, y, width, height);
    }
}
